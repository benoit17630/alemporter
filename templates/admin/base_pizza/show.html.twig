{% extends 'base.html.twig' %}

{% block title %}BasePizza{% endblock %}

{% block body %}
    <h1>BasePizza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ base_pizza.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ base_pizza.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_base_pizza_index') }}">back to list</a>

    <a href="{{ path('admin_base_pizza_edit', {'id': base_pizza.id}) }}">edit</a>

    {{ include('admin/base_pizza/_delete_form.html.twig') }}
{% endblock %}
