{% extends 'base.html.twig' %}

{% block title %}Cheese{% endblock %}

{% block body %}
    <h1>Cheese</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cheese.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cheese.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cheese_index') }}">back to list</a>

    <a href="{{ path('admin_cheese_edit', {'id': cheese.id}) }}">edit</a>

    {{ include('admin/cheese/_delete_form.html.twig') }}
{% endblock %}
