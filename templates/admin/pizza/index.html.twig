{% extends 'base.html.twig' %}

{% block title %}Pizza index{% endblock %}

{% block body %}
    <h1>Pizza index</h1>

    <table class="table">
        <thead>
            <tr>

                <th>Name</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pizza in pizzas %}
            <tr>

                <td>{{ pizza.name }}</td>
                <td>{{ pizza.price }}</td>
                <td>
                    <a href="{{ path('admin_pizza_show', {'id': pizza.id}) }}">show</a>
                    <a href="{{ path('admin_pizza_edit', {'id': pizza.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_pizza_new') }}">Create new</a>
{% endblock %}
