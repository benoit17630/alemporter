{% extends 'base.html.twig' %}

{% block title %}Legume{% endblock %}

{% block body %}
    <h1>Legume</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ legume.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ legume.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_legume_index') }}">back to list</a>

    <a href="{{ path('admin_legume_edit', {'id': legume.id}) }}">edit</a>

    {{ include('admin/legume/_delete_form.html.twig') }}
{% endblock %}
