{% extends 'base.html.twig' %}

{% block title %}Other{% endblock %}

{% block body %}
    <h1>Other</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ other.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ other.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_other_index') }}">back to list</a>

    <a href="{{ path('admin_other_edit', {'id': other.id}) }}">edit</a>

    {{ include('admin/other/_delete_form.html.twig') }}
{% endblock %}
