{% extends 'base.html.twig' %}

{% block title %}BaseIngredient{% endblock %}

{% block body %}
    <h1>BaseIngredient</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Name</th>
            <td>{{ base_ingredient.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_base_ingredient_index') }}">back to list</a>

    <a href="{{ path('admin_base_ingredient_edit', {'id': base_ingredient.id}) }}">edit</a>

    {{ include('admin/base_ingredient/_delete_form.html.twig') }}
{% endblock %}
